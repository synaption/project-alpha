[pytest]
testpaths = tests
python_files = test_*.py
addopts = -vv -ra --durations=0
markers =
	headless_renderer: integration tests using the fake renderer
	unrendered: logic/data tests that do not instantiate a renderer
